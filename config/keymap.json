// Zed keymap — managed by ~/zed-config repo. Leader = <space>.
// Model + rationale: docs/zed-1.2.5-keymap-guide.md
//
// VERIFIED against Zed 1.2.5's own default keymap (zed repo, v1.2.5 tag,
// cross-checked against the installed binary's embedded assets):
//  - `workspace::CloseActiveDock` closes the FOCUSED dock/panel,
//    side-agnostic. This is the correct close action.
//  - Real panel contexts: ProjectPanel, GitPanel, AgentPanel, Terminal.
//  - Default AgentPanel binds alt-l -> CycleFavoriteModels; our
//    AgentPanel block (user keymap loads last -> wins) overrides it.
//  - Default `GitPanel && ChangesList` binds space -> git::ToggleStaged,
//    so inside a git file row `space g g` is unavailable; `escape`
//    closes the git panel instead.
//
// NO cmd/super bindings — Linux renders cmd as Super (Omarchy-reserved).
// macOS-only keys, if ever needed, go in an `os == macos` block.
[
  // == OPEN / focus — editor focused (vim) ============================
  {
    "context": "Editor && (vim_mode == normal || vim_mode == visual)",
    "bindings": {
      "space space": "file_finder::Toggle",
      "space f f": "file_finder::Toggle",
      "space f r": "file_finder::Toggle",
      "space f p": "projects::OpenRecent",
      "space ;": "command_palette::Toggle",
      "space s s": "outline::Toggle",
      "space s S": "project_symbols::Toggle",
      "space ,": "tab_switcher::Toggle",
      "space u t": "theme_selector::Toggle",

      "space e": "project_panel::ToggleFocus",

      "space /": "buffer_search::Deploy",
      "space s g": "project_search::ToggleFocus",
      "space x x": "diagnostics::Deploy",

      "space b d": "pane::CloseActiveItem",
      "space b o": "pane::CloseInactiveItems",
      "space q": "pane::CloseActiveItem",
      "space c a": "editor::ToggleCodeActions",
      "space c r": "editor::Rename",
      "space c f": "editor::Format",

      // cross-zone focus nav: git (left) <- ctrl-h | ctrl-l -> agent
      // (right). ctrl-j/k still navigate code splits vertically.
      "ctrl-h": "git_panel::FocusChanges",
      "ctrl-l": "agent::ToggleFocus",
      "ctrl-j": ["workspace::ActivatePaneInDirection", "Down"],
      "ctrl-k": ["workspace::ActivatePaneInDirection", "Up"],

      "space g g": "git_panel::ToggleFocus",
      "space g B": "branches::OpenRecent",
      "space g b": "editor::ToggleGitBlame",
      "space g d": "editor::ToggleHunkDiff",
      "] h": "editor::GoToHunk",
      "[ h": "editor::GoToPreviousHunk",

      "space d b": "editor::ToggleBreakpoint",
      "space d B": "editor::EditLogBreakpoint",
      "space d c": "debugger::Continue",
      "space d s": "debugger::Start",
      "space d o": "debugger::StepOver",
      "space d i": "debugger::StepInto",
      "space d O": "debugger::StepOut",
      "space d p": "debugger::Pause",
      "space d t": "debugger::Stop",
      "space d r": "debugger::RerunSession",

      "shift-h": "pane::ActivatePreviousItem",
      "shift-l": "pane::ActivateNextItem"
    }
  },

  {
    "context": "Editor && vim_mode == visual",
    "bindings": {
      "shift-left": "editor::Outdent",
      "shift-right": "editor::Indent"
    }
  },

  // == OPEN / focus — no editor (empty pane); also feeds tooltips =====
  {
    "context": "Workspace && !Editor",
    "bindings": {
      "space e": "project_panel::ToggleFocus",
      "space g g": "git_panel::ToggleFocus"
    }
  },

  // == Global: agent open/close, terminal, dock resize, debug F-keys ==
  // alt-l -> ToggleRightDock: agent is the SOLE right-dock panel, so this
  // one action opens AND closes it — exactly what the agent panel button
  // dispatches. Null the default ctrl-alt-b (its old binding) so the
  // tooltip resolves to alt-l. Null ctrl-shift-g / ctrl-shift-e so the
  // git / project tooltips show our space binds, not Zed defaults.
  {
    "context": "Workspace",
    "bindings": {
      "alt-l": "workspace::ToggleRightDock",
      "alt-q": "git_panel::ToggleFocus",
      "ctrl-alt-b": null,
      "ctrl-b": null,
      "ctrl-shift-g": null,
      "ctrl-shift-e": null,
      "ctrl-/": "terminal_panel::Toggle",
      "ctrl-_": "terminal_panel::Toggle",
      "ctrl-`": "terminal_panel::Toggle",

      // Resize lives in each PANEL context below (ctrl-alt-arrows), not
      // here — so it overrides agent-scroll inside the agent and never
      // touches the Editor's ctrl-alt-left/right subword nav. Only the
      // conflict-free reset stays global.
      "ctrl-alt-0": "workspace::ResetActiveDockSize",
      "space w =": "workspace::ResetActiveDockSize",

      "space d u": "debug_panel::ToggleFocus",
      "f5": "debugger::Continue",
      "ctrl-f5": "debugger::Start",
      "shift-f5": "debugger::Stop",
      "f9": "editor::ToggleBreakpoint",
      "f10": "debugger::StepOver",
      "f11": "debugger::StepInto",
      "shift-f11": "debugger::StepOut"
    }
  },

  // debugger panel (left dock per settings): same pattern as project/git.
  {
    "context": "DebugPanel",
    "bindings": {
      "space d u": "workspace::CloseActiveDock",
      "q": "workspace::CloseActiveDock",
      "escape": "workspace::CloseActiveDock",
      "ctrl-alt-right": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-up": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-left": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-down": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-0": "workspace::ResetActiveDockSize"
    }
  },

  // == CLOSE — focused panel closes via workspace::CloseActiveDock ====
  // Same key that opened it (where Zed leaves the key free), plus
  // `escape` as the universal closer.

  // project tree: `not_editing` so keys don't fire while renaming.
  {
    "context": "ProjectPanel && not_editing",
    "bindings": {
      "space e": "workspace::CloseActiveDock",
      "q": "workspace::CloseActiveDock",
      "escape": "workspace::CloseActiveDock",
      "ctrl-l": "workspace::FocusCenterPane",
      "ctrl-alt-right": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-up": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-left": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-down": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-0": "workspace::ResetActiveDockSize"
    }
  },

  // git: EXACT agent technique. One chord (alt-q) bound at EVERY git
  // context to workspace::ToggleLeftDock (git's own collapse-button
  // action — that's why its tooltip was ctrl-b, which we null). alt-q is
  // a modifier chord with NO default binding in any git context, so it
  // is safe even inside the commit text box. Opening + focus is
  // alt-q -> git_panel::ToggleFocus in the Workspace block.
  // escape/q kept as extra closers where they don't break text editing.
  {
    "context": "GitPanel && ChangesList && !GitBranchSelector",
    "bindings": {
      "alt-q": "workspace::CloseActiveDock",
      "escape": "workspace::CloseActiveDock",
      "q": "workspace::CloseActiveDock",
      "ctrl-k": "git_panel::FocusChanges",
      "ctrl-j": "git_panel::FocusEditor",
      "ctrl-l": "workspace::FocusCenterPane",
      "ctrl-alt-right": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-up": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-left": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-down": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-0": "workspace::ResetActiveDockSize"
    }
  },
  {
    "context": "GitPanel && !CommitEditor",
    "bindings": {
      "alt-q": "workspace::CloseActiveDock",
      "escape": "workspace::CloseActiveDock",
      "q": "workspace::CloseActiveDock",
      "ctrl-k": "git_panel::FocusChanges",
      "ctrl-j": "git_panel::FocusEditor",
      "ctrl-l": "workspace::FocusCenterPane",
      "ctrl-alt-right": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-up": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-left": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-down": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-0": "workspace::ResetActiveDockSize"
    }
  },
  {
    "context": "GitPanel && CommitEditor",
    "bindings": {
      "alt-q": "workspace::CloseActiveDock",
      "ctrl-k": "git_panel::FocusChanges",
      "ctrl-j": "git_panel::FocusEditor"
    }
  },
  {
    "context": "CommitEditor > Editor",
    "bindings": {
      "alt-q": "workspace::CloseActiveDock",
      "ctrl-k": "git_panel::FocusChanges",
      "ctrl-j": "git_panel::FocusEditor"
    }
  },
  {
    "context": "GitCommit > Editor",
    "bindings": {
      "alt-q": "workspace::CloseActiveDock",
      "ctrl-k": "git_panel::FocusChanges",
      "ctrl-j": "git_panel::FocusEditor"
    }
  },
  {
    "context": "GitPanel",
    "bindings": {
      "alt-q": "workspace::CloseActiveDock",
      "ctrl-k": "git_panel::FocusChanges",
      "ctrl-j": "git_panel::FocusEditor",
      "ctrl-l": "workspace::FocusCenterPane",
      "ctrl-alt-right": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-up": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-left": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-down": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-0": "workspace::ResetActiveDockSize"
    }
  },

  // agent: alt-l -> ToggleRightDock at EVERY agent context (close works
  // regardless of focus depth; agent is the sole right-dock panel).
  // RIGHT dock: grow = edge moves LEFT, so ctrl-alt-left = Increase,
  // ctrl-alt-right = Decrease (inverse of left-side docks).
  {
    "context": "AgentPanel",
    "bindings": {
      "alt-l": "workspace::ToggleRightDock",
      "escape": "workspace::ToggleRightDock",
      "ctrl-h": "workspace::FocusCenterPane",
      "ctrl-alt-left": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-up": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-right": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-down": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-0": "workspace::ResetActiveDockSize"
    }
  },
  {
    "context": "AgentPanel && acp_thread",
    "bindings": {
      "alt-l": "workspace::ToggleRightDock",
      "ctrl-h": "workspace::FocusCenterPane",
      "ctrl-alt-left": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-up": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-right": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-down": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-0": "workspace::ResetActiveDockSize"
    }
  },
  {
    "context": "AcpThread",
    "bindings": {
      "alt-l": "workspace::ToggleRightDock",
      "ctrl-h": "workspace::FocusCenterPane",
      "ctrl-alt-left": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-up": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-right": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-down": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-0": "workspace::ResetActiveDockSize"
    }
  },
  {
    "context": "AcpThread > Editor",
    "bindings": {
      "alt-l": "workspace::ToggleRightDock",
      "ctrl-h": "workspace::FocusCenterPane",
      "ctrl-alt-left": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-up": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-right": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-down": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-0": "workspace::ResetActiveDockSize"
    }
  },

  // terminal: terminal_panel::Toggle is a TRUE open/close toggle and the
  // terminal is the sole bottom-dock panel, so this works both ways and
  // focuses the terminal on open (agent technique). Bound in the
  // Terminal context too so it also closes from INSIDE the terminal —
  // the default Terminal context captures many ctrl- keys for the shell,
  // so this explicit override is required (that is why ctrl-/ "did
  // nothing" before).
  {
    "context": "Terminal",
    "bindings": {
      "ctrl-/": "terminal_panel::Toggle",
      "ctrl-_": "terminal_panel::Toggle",
      "ctrl-`": "terminal_panel::Toggle",
      "ctrl-alt-up": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-right": ["workspace::IncreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-down": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-left": ["workspace::DecreaseActiveDockSize", { "px": 48 }],
      "ctrl-alt-0": "workspace::ResetActiveDockSize"
    }
  }
]
