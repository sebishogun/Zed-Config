// Zed keymap — managed by ~/zed-config repo. Leader = <space>.
//
// Design: project tree + agent stay as DOCKS; everything else opens as a
// CENTERED PALETTE/picker. Actions verified against Zed 1.2.5 default
// keymaps (linux/macos/windows). If a binding errors after a Zed upgrade,
// run `zed: open default keymap` and copy the current action name.
//
// Zed limits (not bypassable in 1.2.5):
//  - One key = one action; "open+focus AND close" needs the two context
//    blocks below (editor opens+focuses, panel-focused closes).
//  - No centered palette for git status/staging (git_panel dock only) or
//    diagnostics/project-search (open as editor tabs / top bar).
[
  // -- editor: leader palettes, nav, git, debugger ----------------------
  {
    "context": "Editor && (vim_mode == normal || vim_mode == visual)",
    "bindings": {
      // centered pickers / palettes
      "space space": "file_finder::Toggle",
      "space f f": "file_finder::Toggle",
      "space f r": "file_finder::Toggle",
      "space f p": "projects::OpenRecent",
      "space ;": "command_palette::Toggle",
      "space s s": "outline::Toggle",
      "space s S": "project_symbols::Toggle",
      "space ,": "tab_switcher::Toggle",
      "space u t": "theme_selector::Toggle",

      // project tree dock (open + focus; close handled in ProjectPanel ctx)
      "space e": "project_panel::ToggleFocus",

      // search (Zed: top bar / pane, not a centered palette)
      "space /": "buffer_search::Deploy",
      "space s g": "project_search::ToggleFocus",

      // diagnostics (Zed: opens as an editor tab, not a palette)
      "space x x": "diagnostics::Deploy",

      // buffers / panes
      "space b d": "pane::CloseActiveItem",
      "space b o": "pane::CloseInactiveItems",
      "space q": "pane::CloseActiveItem",
      "space c a": "editor::ToggleCodeActions",
      "space c r": "editor::Rename",
      "space c f": "editor::Format",

      // pane/split navigation (nvim ctrl-w hjkl)
      "ctrl-h": ["workspace::ActivatePaneInDirection", "Left"],
      "ctrl-j": ["workspace::ActivatePaneInDirection", "Down"],
      "ctrl-k": ["workspace::ActivatePaneInDirection", "Up"],
      "ctrl-l": ["workspace::ActivatePaneInDirection", "Right"],

      // git: status = left dock (no centered palette in Zed);
      //      branches = centered palette
      "space g g": "git_panel::ToggleFocus",
      "space g B": "branches::OpenRecent",
      "space g b": "editor::ToggleGitBlame",
      "space g d": "editor::ToggleHunkDiff",
      "] h": "editor::GoToHunk",
      "[ h": "editor::GoToPreviousHunk",

      // debugger (LazyVim-style <leader>d*)
      "space d b": "editor::ToggleBreakpoint",
      "space d c": "debugger::Continue",
      "space d s": "debugger::Start",
      "space d o": "debugger::StepOver",
      "space d i": "debugger::StepInto",
      "space d O": "debugger::StepOut",
      "space d p": "debugger::Pause",
      "space d t": "debugger::Stop",
      "space d r": "debugger::RerunSession",

      // tab nav (LazyVim S-h / S-l)
      "shift-h": "pane::ActivatePreviousItem",
      "shift-l": "pane::ActivateNextItem"
    }
  },

  // -- project panel focused (not renaming): space e / q CLOSES dock ---
  {
    "context": "ProjectPanel && not_editing",
    "bindings": {
      "space e": "workspace::ToggleLeftDock",
      "q": "workspace::ToggleLeftDock",
      "ctrl-h": ["workspace::ActivatePaneInDirection", "Left"],
      "ctrl-j": ["workspace::ActivatePaneInDirection", "Down"],
      "ctrl-k": ["workspace::ActivatePaneInDirection", "Up"],
      "ctrl-l": ["workspace::ActivatePaneInDirection", "Right"]
    }
  },

  // -- git panel focused (not the commit box): space g g / q CLOSES ---
  {
    "context": "GitPanel && !CommitEditor",
    "bindings": {
      "space g g": "workspace::ToggleLeftDock",
      "q": "workspace::ToggleLeftDock"
    }
  },

  // -- agent panel focused (right dock): alt-l closes it ---------------
  // No `q` binding — that would block typing 'q' in the chat input.
  {
    "context": "AgentPanel",
    "bindings": {
      "alt-l": "workspace::ToggleRightDock",
      "cmd-l": "workspace::ToggleRightDock"
    }
  },

  // -- terminal focused: ctrl-/ etc. closes the terminal panel --------
  {
    "context": "Terminal",
    "bindings": {
      "ctrl-/": "terminal_panel::Toggle",
      "ctrl-_": "terminal_panel::Toggle",
      "ctrl-`": "terminal_panel::Toggle",
      "cmd-/": "terminal_panel::Toggle",
      "cmd-`": "terminal_panel::Toggle"
    }
  },

  // -- visual-mode indent keeps selection (nvim S-Left/Right) ----------
  {
    "context": "Editor && vim_mode == visual",
    "bindings": {
      "shift-left": "editor::Outdent",
      "shift-right": "editor::Indent"
    }
  },

  // -- pane nav from any focused dock back to the editor ---------------
  {
    "context": "Dock",
    "bindings": {
      "ctrl-h": ["workspace::ActivatePaneInDirection", "Left"],
      "ctrl-j": ["workspace::ActivatePaneInDirection", "Down"],
      "ctrl-k": ["workspace::ActivatePaneInDirection", "Up"],
      "ctrl-l": ["workspace::ActivatePaneInDirection", "Right"]
    }
  },

  // -- workspace-level panel toggles (NOT in a text input) ------------
  // Mirrors the editor leader binds so panel tooltips show these keys.
  // `!Editor` keeps the space-leader out of search / branch-name /
  // rename boxes (those are all Editors in Zed). Panel-focused close
  // binds above are more specific, so they still win.
  {
    "context": "Workspace && !Editor",
    "bindings": {
      "space e": "project_panel::ToggleFocus",
      "space g g": "git_panel::ToggleFocus"
    }
  },

  // -- global: agent dock, terminal, debugger F-keys ------------------
  {
    "context": "Workspace",
    "bindings": {
      "alt-l": "agent::ToggleFocus",
      "cmd-l": "agent::ToggleFocus",
      "ctrl-/": "terminal_panel::Toggle",
      "ctrl-_": "terminal_panel::Toggle",
      "ctrl-`": "terminal_panel::Toggle",
      "cmd-/": "terminal_panel::Toggle",
      "cmd-`": "terminal_panel::Toggle",

      "f5": "debugger::Continue",
      "ctrl-f5": "debugger::Start",
      "shift-f5": "debugger::Stop",
      "f9": "editor::ToggleBreakpoint",
      "f10": "debugger::StepOver",
      "f11": "debugger::StepInto",
      "shift-f11": "debugger::StepOut"
    }
  }
]
